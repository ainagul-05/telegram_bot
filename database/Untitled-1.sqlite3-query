-- database: ./bot.db

-- Use the ▷ button in the top right corner to run the entire file.

SELECT * FROM products_detail;

SELECT * FROM products;

INSERT INTO products(name, price, product_id, photo) 
VALUES('nike', 6500, '1000','https://');

INSERT INTO products_detail(description, product_id, category) 
VALUES('Cool nike shoes', '1000', 'Shoes');



SELECT products.name, products.price, products_detail.description, products_detail.category, products.product_id, products_detail.product_id
FROM products
INNER JOIN products_detail on products.product_id = products_detail.product_id; #только то что есть в обеих таблицах


SELECT products.name, products.price, products_detail.description, products_detail.category, products.product_id, products_detail.product_id
FROM products
LEFT JOIN products_detail on products.product_id = products_detail.product_id; #все из первой таблицы и то что есть в левой , если нет то напишет null

SELECT products.name, products.price, products_detail.description, products_detail.category, products.product_id, products_detail.product_id
FROM products
RIGHT JOIN products_detail on products.product_id = products_detail.product_id; #все из второй таблицы и то что есть в первой , если нет то напишет null


SELECT products.name, products.price, products_detail.description, products_detail.category, products.product_id, products_detail.product_id
FROM products
FULL JOIN products_detail on products.product_id = products_detail.product_id; #все из обеих таблиц, даже если нет совпадений


UPDATE products SET name = 'Reebok' products.product_id =  1001; 



